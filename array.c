#include<stdio.h>
#define MAXMIN
void main()
{	
int MAX=a[0],MIN=a[0],sum=0,i,n=10;

for(i=1;i<n;i++)
{
if(a[i]>MAX)
printf("a[i] is greater than MAX");
}
 max=a[i]

for(i=1,i<n,i++)
{
if(a[i]<MIN)
printf("a is less than MIN");
}
MIN=a[i]
printf("%d\n",MAX[i],MIN[i]);
}







